import DocCardList from '@theme/DocCardList';

# 🗽 Models

The classes in this module are wrappers for the `Chat` classes within
[`LangChain`](https://www.langchain.com) that provides default configuration and
could be extended with additional methods if needed.

<DocCardList />
