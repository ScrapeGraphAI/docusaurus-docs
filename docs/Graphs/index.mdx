import DocCardList from '@theme/DocCardList';

# 🕸 Graphs

In the standard structure of `Scrapegraph-ai`, a graph is composed of [nodes](/docs/Nodes),
each with its own functionality. This section contains a list of the currently supported graph types.

<DocCardList />
